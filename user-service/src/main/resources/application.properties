# Server
server.port=${USER_SVC_PORT:8082}

# Datasource (dùng env, có default ?? ch?y local)
spring.datasource.url=${DB_URL:jdbc:postgresql://localhost:5432/user_service}
spring.datasource.username=${DB_USERNAME:postgres}
spring.datasource.password=${DB_PASSWORD:password}
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# JWT secret (env tr??c, fallback local)
security.jwt.secret=${JWT_SECRET:w5h2Yk1Gd3dtdkFzT0h0bTFwM05XUnJvV0V1NHVZbU5yZFRyWW9oUUNmQ1hLcw==}

# API base path
api.base-path=${API_BASE_PATH:/api/v1}
