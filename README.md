# User Access Management (Multi-Module)

## Modules
- core (library dùng chung)
- auth-service (port 8081) – `/api/auth/hello`
- user-service (port 8082) – `/api/users/hello`

## Build
```bash
./mvnw clean install
